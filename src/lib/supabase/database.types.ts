// Types générés depuis le schéma Supabase.
// TODO (après chaque migration) : régénérer avec
//   npx supabase gen types typescript --project-id <ref> > src/lib/supabase/database.types.ts
// En attendant, type permissif (any) pour ne pas bloquer la fondation v0 : les
// requêtes ne sont PAS typées tant que database.types.ts n'est pas régénéré.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
